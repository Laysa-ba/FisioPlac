<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FisioPlac"
        tools:targetApi="31">

        <!-- *** CORREÇÃO AQUI *** -->
        <!-- Movemos a GeriatricaActivity para seu novo pacote -->
        <activity
            android:name=".ui.form_geriatrica.GeriatricaActivity"
            android:exported="false" />

        <!-- Activities ainda no pacote raiz -->
        <activity
            android:name=".Tela8FichaActivity"
            android:exported="false" />
        <activity
            android:name=".Tela1FichaActivity"
            android:exported="false" />

        <!-- Activities refatoradas -->
        <activity
            android:name=".ui.newfile.NewFileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />

        <!-- Meta-data do AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

    </application>

</manifest>